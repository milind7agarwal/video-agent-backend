# ── Lightweight Render deploy requirements ─────────────────────────────
# No torch, no local whisper, no chromadb, no sentence-transformers, no streamlit.
# Transcription and embeddings both go through hosted APIs instead.

fastapi>=0.110.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
requests>=2.32.0
pydub>=0.25.1
audioop-lts>=0.1.0; python_version>='3.13'
yt-dlp>=2024.4.9

# LLM + embeddings via Mistral's hosted API (no local models)
langchain>=0.2.0
langchain-core>=0.2.0
langchain-mistralai>=0.1.0
langchain-community
langchain-text-splitters
mistralai>=0.4.0

numpy>=1.26.0